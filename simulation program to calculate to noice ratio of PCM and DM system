clc,

clear all;

close all

v=input('Enter number of bits:');

fm =input('Enter input signal frequency: ");

Ts=input('Enter sampling interval: ');

SNQPCMdB-1.8+6*v;

SNQDM (3/(8*(2*3.14) (3*fm)(3*Ts)));

display (SNQPCMdB);

display (SNQDM);
